fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
python-dotenv==1.0.1
pydantic==2.9.2

# RAG stack — embeddings + Chroma local; answers via Groq API
sentence-transformers==3.1.1
chromadb==0.5.5
# chromadb 0.5.x breaks with posthog 3+/6+/7+ (noisy capture() telemetry errors)
posthog>=2.4.0,<3
